# PAI Instance Metadata
# This file tracks information about this PAI installation

# Git repository for syncing across devices (optional)
# If set, `pai sync` will use git pull/push
repo: git@github.com:justintieu/pai.git

# Sync method: git | cloud | manual
sync_method: git

# Owner info (helps AI understand whose PAI this is)
owner:
  name: null
  # Add any other identity hints here

# Maintenance tracking (auto-updated by `pai maintain`)
last_maintained: 2026-01-27T22:55:13Z
maintenance_interval_days: 7  # Run maintenance if older than this

# Notes about this PAI instance
notes: |
  Created by `pai setup` on initial setup.
  Edit this file to configure sync settings.
